services:
  works-api:
    build: .
    container_name: works-api
    restart: unless-stopped
    ports:
      - "8788:8788"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      WORKS_HOST: "0.0.0.0"
      WORKS_PORT: "8788"
